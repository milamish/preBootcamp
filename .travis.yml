language: python

python:
  - '3.4'
services:
  - postgresql
install:
  - pip install -r requirements.txt
  - pip install pytest-cov
  - pip install coveralls
before_script:
  - cd API/api_v2/tests
  - psql -c 'create database questions;' -U postgres
script:
  - pytest
  - pytest --cov=.

after_success:
  - coveralls

cache: pip